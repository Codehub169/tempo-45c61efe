// components/Layout.js
import React from 'react';

const Layout = ({ children }) => {
  return (
    <>
      {/* Future: Add common layout elements like Header, Footer here */}
      {/* For now, just render children. Header/Footer can be added later. */}
      {children}
    </>
  );
};

export default Layout;
